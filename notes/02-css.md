# CSS

MAKING VARIABLES:

:root{
  --var: ex;
}

EX: Colors

- :root{} creates variables that all elements and classes have access to

- var() is how you pull access to variables in CSS

display: FLEX; setting an element to flex affects all the children of an element as well, sets the children to a horizontal layout,p rather than vertical. - use on body
  flex-direction: 
  flex-grow: ratios based on number value - higher numbers means more importance, more page room

  min-height - regardless of how tall the content is, the minimum height is N%vh vh = view height

  .class-selector-name: 